{% for server in openio_zookeeper_servers -%}
{% if server.ip is defined -%}
{% if server.ip in ansible_all_ipv4_addresses -%}
{{ server.id }}
{% endif -%}
{% else -%}
{{ openio_zookeeper_servers.index(inventory_hostname) + 1 }}
{% endif -%}
{% endfor -%}
